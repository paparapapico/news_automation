# Railway 배포용 requirements.txt - MoviePy 완전 제거
# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Authentication & Security
PyJWT==2.8.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP & Async
aiohttp==3.9.1
aiofiles==23.2.0
httpx==0.25.2
requests==2.31.0

# Web Scraping
beautifulsoup4==4.12.2
lxml==4.9.3
feedparser==6.0.10

# File Handling
python-multipart==0.0.6
python-dotenv==1.0.0

# Image Processing (OpenCV만 유지)
opencv-python-headless==4.8.1.78
Pillow==10.1.0

# AI & ML
openai==1.3.7

# Data Processing
pandas==2.1.4
numpy==1.24.4

# Audio Processing (최소화)
gtts==2.4.0

# Production Server
gunicorn==21.2.0

# 제거된 라이브러리들 (Railway 환경에서 문제 발생):
# - moviepy (의존성 문제)
# - imageio (moviepy 의존성)
# - imageio-ffmpeg (ffmpeg 의존성)
# - pydub (오디오 의존성)
# - speechrecognition (마이크 의존성)
# - pygame (GUI 의존성)
# - mutagen (오디오 메타데이터)